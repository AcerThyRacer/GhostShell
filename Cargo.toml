[package]
name = "ghostshell"
version = "0.1.0"
edition = "2021"
authors = ["GhostShell Contributors"]
description = "A modern shell written in Rust for Windows and Linux"
license = "MIT"

[dependencies]
rustyline = "14.0"

[profile.release]
opt-level = 3
lto = true
